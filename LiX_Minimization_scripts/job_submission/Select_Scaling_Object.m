function Scaling = Select_Scaling_Object(Scale,index)

idx = 1;
Scales = fieldnames(Scale);
for cur_scale = Scales'
    Interactions = fieldnames(Scale.(cur_scale{1}));
    for Interaction = Interactions'
        
    end
end




end